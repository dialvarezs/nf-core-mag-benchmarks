[project]
name = "nf-core-mag-benchmarks"
version = "0.1.0"
description = "Benchmarking data analysis for the nf-core/mag pipeline"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "notebook>=7.6.1",
    "plotnine>=0.15.7",
    "polars>=1.43.0",
    "pyarrow>=25.0.0",
]
